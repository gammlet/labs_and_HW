#include <stdio.h>
#include <stdlib.h>

int make_grid(int n ) {

    int oneAxis = 2*n +1;
    int total = oneAxis*oneAxis*oneAxis;

    for (int i = 1; i <= n; i++) {
        int *arr = malloc(n*sizeof(int));
    }
}


int main(void) {

}